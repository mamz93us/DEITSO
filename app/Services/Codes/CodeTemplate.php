<?php

declare(strict_types=1);

namespace App\Services\Codes;

/**
 * Reserved seam — per-org configurable templates land here in a later sprint
 * (PROJECT.md Section 11). For now a placeholder so the generator API is stable.
 */
class CodeTemplate
{
    //
}
